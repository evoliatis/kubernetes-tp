apiVersion: v1
kind: ResourceQuota
metadata:
  name: quota
  labels:
    app: stress
  namespace: stress
spec:
  hard:
    requests.memory: 32Mi
    requests.cpu: "50m"
    limits.memory: 64Mi
    limits.cpu: "100m"  
