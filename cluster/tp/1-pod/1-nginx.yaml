apiVersion: v1
kind: Pod
metadata:
  name: my-pod-nginx
  labels:
    app: nginx
spec:
  containers:
  - name: nginx-demo
    image: evoliatis/nginx
    ports:
    - name: nginx-port
      containerPort: 80
