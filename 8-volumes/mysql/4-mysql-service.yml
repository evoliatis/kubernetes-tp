apiVersion: v1
kind: Service
metadata:
  name: mysql-service
spec:
  ports:
  - port: 31500
    nodePort: 31500
    targetPort: mysql-port
    protocol: TCP
  selector:
    app: mysql
  type: NodePort
